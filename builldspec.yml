version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies"
      - npm install
  build:
    commands:
      - echo "Build skipped - no build script in package.json"
      - npm start  # Start the app directly using the start script

artifacts:
  base-directory: simple-app
  files:
    - '**/*'
